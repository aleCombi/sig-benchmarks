[project]
name = "sig-benchmarks"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "chen-signatures",
    "iisignature",
    "pysiglib",
    "numpy",
    "torch",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
